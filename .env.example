# Copy to .env and fill in (never commit .env)
#
# VS Code Marketplace: Azure DevOps PAT with "Marketplace → Manage"
# https://dev.azure.com → User settings → Personal access tokens
VSCE_PAT=

# Open VSX (Cursor / VSCodium): https://open-vsx.org/user-settings/tokens
OVSX_PAT=
